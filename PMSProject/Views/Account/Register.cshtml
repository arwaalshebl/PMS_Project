@model PMSProject.Models.LoginViewModel

@{
    ViewData["Title"] = "Register";
}

<div class="container-fluid vh-100">
    <div class="row vh-100">


        <div class="col-md-6 d-flex align-items-center justify-content-center bg-light">
            <div class="col-10 col-sm-8 col-lg-7">


                <div class="mb-4">
                    <h3 class="fw-bold">Create Account</h3>
                    <p class="text-muted">Sign up today and start learning from the experts.</p>
                </div>

                <form asp-action="Register" method="post">
                    <div asp-validation-summary="ModelOnly" class="text-danger small mb-3"></div>
                    <!-- User Name Field -->
                    <div class="form-floating mb-3">
                        <input asp-for="UserName" class="form-control border-0 shadow-sm" id="regUserName" placeholder="name" style="border-radius: 12px;">
                        <label asp-for="UserName" for="regEmail" class="text-muted">User Name</label>
                        <span asp-validation-for="Email" class="text-danger small"></span>
                    </div>
                    <!-- Email Field -->
                    <div class="form-floating mb-3">
                        <input asp-for="Email" class="form-control border-0 shadow-sm" id="regEmail" placeholder="name@example.com" style="border-radius: 12px;">
                        <label asp-for="Email" for="regEmail" class="text-muted">Email Address</label>
                        <span asp-validation-for="Email" class="text-danger small"></span>
                    </div>

                    <!-- Password Field -->
                    <div class="form-floating mb-4">
                        <input asp-for="Password" type="password" class="form-control border-0 shadow-sm" id="regPassword" placeholder="Password" style="border-radius: 12px;">
                        <label asp-for="Password" for="regPassword" class="text-muted">Password</label>
                        <span asp-validation-for="Password" class="text-danger small"></span>
                    </div>

                    <!-- Register Button -->
                    <button type="submit" class="btn btn-primary w-100 py-3 shadow" style="border-radius: 12px; background-color: #00419e; border: none;">
                        Join for Free
                    </button>

                    <p class="mt-3 small text-center text-muted">
                        By clicking "Join for Free", you agree to our <a href="#" class="text-decoration-none">Terms of Use</a>.
                    </p>
                </form>

                <div class="text-center mt-4">
                    <p class="text-muted">
                        Already have an account?
                        <a asp-action="Login" class="text-primary fw-bold text-decoration-none">Log in</a>
                    </p>
                </div>

            </div>
        </div>
    </div>
</div>
